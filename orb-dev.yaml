# Orbstack development environment values for CallTelemetry deployment

# Environment information
env:
  namespace: ct-dev
  environment: dev
  domain: ct-dev.k8s.orb.local

# HAProxy configuration
haproxy:
  values_file: examples/haproxy-ct-dev-loadbalancer-values.yaml

# Ingress configuration
ingress:
  values_file: examples/ingress-ct-dev-orbstack-values.yaml

# API configuration
api:
  values_file: examples/api-ct-dev-values.yaml

# Vue Web configuration
vue_web:
  values_file: examples/vue-web-ct-dev-values.yaml

# Teams Auth configuration
teams_auth:
  values_file: examples/teams-auth-ct-dev-values.yaml

# Traceroute configuration
traceroute:
  values_file: examples/traceroute-ct-dev-values.yaml

# Echo configuration
echo:
  values_file: examples/echo-haproxy-ct-dev-values.yaml
