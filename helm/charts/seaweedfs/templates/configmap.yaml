{{- /*
  S3 identity config is now in s3-config-secret.yaml (Secret) instead of ConfigMap.
  Credentials should never be stored in a ConfigMap — ConfigMaps are not subject
  to RBAC secret restrictions and are visible to anyone with pod read access.
*/ -}}
