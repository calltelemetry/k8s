apiVersion: v1
kind: Service
metadata:
  name: {{ include "vue-web.fullname" . }}
  labels:
    {{- include "vue-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vue-web.selectorLabels" . | nindent 4 }}
