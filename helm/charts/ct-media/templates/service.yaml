apiVersion: v1
kind: Service
metadata:
  name: {{ include "ct-media.fullname" . }}
  labels:
    {{- include "ct-media.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "ct-media.selectorLabels" . | nindent 4 }}
