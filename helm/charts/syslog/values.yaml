replicaCount: 1

image:
  repository: calltelemetry/ct-syslog-ingest
  tag: latest
  pullPolicy: IfNotPresent

# Container listen ports. Defaults bind the privileged syslog port 514 (prod
# runs where CAP_NET_BIND_SERVICE is effective). Previews override udp/tcp to an
# unprivileged port (e.g. 1514) because the capability is dropped on exec for
# non-root pods; the Service below still presents 514 and maps to these.
ports:
  udp: 514
  tcp: 514
  health: 5514

service:
  # UDP service for syslog reception
  udp:
    type: ClusterIP
    port: 514
  # TCP service for health/metrics
  health:
    type: ClusterIP
    port: 5514

# Loki log aggregation endpoint
loki:
  endpoint: "http://loki:3100/loki/api/v1/push"

# NATS for critical alert routing (optional)
nats:
  url: "nats://nats:4222"

# OpenTelemetry (optional)
otel:
  enabled: false
  endpoint: "http://otel-collector:4318"

# Prometheus ServiceMonitor
serviceMonitor:
  enabled: true
  interval: 15s

resources:
  limits:
    cpu: "1"
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

securityContext:
  runAsUser: 1000
  runAsGroup: 1000
  runAsNonRoot: true
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]
    add: ["NET_BIND_SERVICE"]

nodeSelector: {}
tolerations: []
affinity: {}
