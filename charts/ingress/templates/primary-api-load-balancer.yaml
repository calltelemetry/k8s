apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-primary-api-external
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-primary-api
  annotations:
    kubernetes.io/ingress.class: nginx
    {{- if eq .Values.loadBalancerType "onprem" }}
    metallb.universe.tf/address-pool: {{ .Values.environment }}-primary-ip
    {{- end }}
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster
  selector:
    app: nginx-ingress-controller
  ports:
    - port: {{ .Values.networking.api_port }}
      targetPort: 80
      protocol: TCP
      name: web
    - port: 443
      targetPort: 443
      protocol: TCP
      name: https
