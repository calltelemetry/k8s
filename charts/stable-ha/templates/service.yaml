apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name  }}-web
  labels:
    app: {{ .Release.Name  }}-web
spec:
  selector:
    app: {{ .Release.Name  }}-web
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
      name: http